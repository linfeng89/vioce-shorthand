# 语音日记 App — 项目需求文档

> 最后更新：2026-05-01
> 状态：需求沉淀中，未进入开发

---

## 一、产品定位

**一句话描述**：按住录音，松开看字。离线、本地、极简的语音日记工具。

**核心理念**：快进快出，零摩擦记录。随时随地，碎片化捕获想法。

**目标用户**：想记日记但嫌打字麻烦的人。场景覆盖：走路、排队、开会间隙、睡前——任何灵感闪现的瞬间。

**设计哲学**：
- **快**：打开即录，松手即存，一步都不能多
- **安全**：录音落盘优先于一切，转写是锦上添花
- **离线**：不依赖网络，地铁/飞机/山区都能用

---

## 二、核心交互流程

```
打开 App → 看到录音按钮（首页即录音页）
        → 按住录音（短按 tap 也可开始/停止，适配长录音场景）
        → 松开 → 【立即保存录音 + 写入数据库】→ 日记已存在
        → 后台异步转文字（sherpa-onnx 离线推理）
        → 转写完成 → 更新文字内容 → UI 自动刷新
        → 可手动修正文字 / 重新转写
```

**设计原则**：打开即录，零步骤进入录音状态。

**关键设计**：录音保存与语音转写是两个独立步骤。用户松手的瞬间，日记就已经安全落盘，转写是"后补"的增强体验。即使转写失败或 App 被杀掉，原始录音不会丢失。

**录音交互细节**：

> 核心设计：按住 + 上滑锁定（微信/WhatsApp 已教育过的手势，零学习成本）

- **按住录音**：按住按钮开始录音，松开停止并保存（最快，适合短记录）
- **上滑锁定**：按住后上滑超过阈值 → 触觉振动 → 锁定录音（松手不停止）
- **锁定后**：按钮变为「点击停止」+「取消」，用户自由控制结束时机
- **取消手势**：按住后上滑到屏幕上方取消区域 → 松开取消录音（不保存）
- **短按过滤**：短按（< 0.3s）视为误触，不启动录音，无反馈
- **触觉反馈**：开始录音时短振动，锁定时再振动一次
- **录音中 UI**：计时器 + 音量指示 + 状态提示（「松开停止」/「锁定中·点击停止」）

**录音状态机**：

```
按住按钮
  ├── 短按 (< 0.3s) 松开 → 误触过滤，无操作
  ├── 按住 (> 0.3s) 不放 → 录音中
  │   ├── 上滑超过阈值 → 🔒 锁定（松手不停止，点击停止按钮结束）
  │   │   ├── 点击停止 → 保存
  │   │   └── 点击取消 → 取消录音
  │   ├── 上滑到取消区域 → 取消录音
  │   └── 直接松开 → 停止并保存（短录音）
  └── 无操作 → 保持录音
```

---

## 三、功能清单

### MVP（P0）

| 功能 | 说明 |
|------|------|
| 录音 | 按住录音 / 松开停止，PCM 直写 WAV 格式（最低延迟），支持取消手势 |
| 离线语音转文字 | Whisper ONNX 端侧推理，中文识别 |
| 转写结果展示 | 录完自动显示转写文字，支持手动编辑修正，支持重新转写 |
| 日记列表 | 按天倒序展示，每天内按时间段自动分组 |
| 音频回放 | 点击日记条目可播放原始录音 |
| 全文搜索 | 搜索转写文字内容（SQLite FTS5 + 简单 CJK 双字符分词），未转写条目不纳入搜索结果，支持按日期范围过滤 |
| 删除 | 左滑删除 → 移入回收站（软删除），回收站保留 30 天后自动物理删除，支持从回收站恢复 |
| 本地手动备份 | 设置页「立即备份」按钮，将 SQLite 数据库 + 音频文件打包到 Documents 目录，只保留最新一份，支持从备份恢复 |

### 后续迭代

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P1 | 快捷录音 | 通知栏按钮（常驻）+ 桌面 Widget（首选）+ 耳机触发（P1，取决于平台支持） |
| P1 | 分享单条日记 | 长按条目 → 分享为文字 / 音频 / 文字卡片 |
| P1 | 导出日记 | TXT / Markdown / JSON + 音频打包导出 |
| P2 | 日历视图 | 以月历形式浏览有录音的日期 |
| P2 | 情绪/心情标签 | 从转写文字自动提取情绪（AI 生成，非手动选择） |
| P2 | 统计与习惯 | 连续记录天数、累计时长、每周频率、词云 |
| P2 | 暗黑模式 | 适配系统深色模式 |
| P2 | 云端同步 | iCloud / OneDrive 增量同步（详见存储空间管理章节），辅助功能后期完善 |
| P3 | AI 摘要 | 自动生成一句话摘要 |
| P3 | 标签系统 | 自动从内容提取关键词做标签 |

### 可选功能：多段录音合并

> ⚠️ **可选功能（后期迭代）**：数据模型已从 MVP 预留（`AudioSegment` 表），UI 和业务逻辑是否开发待定。

**场景**：录到一半被中断 / 想追加补充 / 对某段不满意想重录，但不想新建一篇日记。

**功能定义**：

| 操作 | 说明 | 交互 |
|------|------|------|
| 追加录音 | 往已有日记追加新段落 | 日记详情页 → 点击「追加录音」→ 进入录音 → 录完追加到该日记 |
| 重录 | 对某段不满意，重新录制 | 日记详情页 → 点击「重录」→ 确认 → 录完替换主音频，原音频保留为备份 |
| 合并播放 | 按段顺序连续播放 | 播放器显示段落标记，段间 0.5s 静音，可跳转 |
| 合并文字 | 多段转写结果拼接 | 段间换行分隔，用户可编辑合并后的完整文字 |

**数据模型（MVP 预留）**：

```csharp
// MVP 时创建表，但 UI 不暴露
public class AudioSegment
{
    public string Id { get; set; }            // GUID
    public string EntryId { get; set; }       // 关联的日记 ID
    public string AudioFileName { get; set; } // 音频文件名
    public int SegmentIndex { get; set; }     // 段序号（0, 1, 2...）
    public int DurationSeconds { get; set; }  // 该段时长
    public DateTime RecordedAt { get; set; }  // 录制时间
    public string TranscribedText { get; set; } // 该段单独的转写文字
    public SegmentType Type { get; set; }     // Original / Append / ReRecord
}
```

**文件命名**：`{日期}_{时间}_{序号}_seg{段号}.wav`，如 `20260501_0932_001_seg0.wav`

**开发节奏**：

| 阶段 | 内容 |
|------|------|
| MVP | 仅建 `AudioSegment` 表，UI 不暴露，录音同时写入 Segment 记录 |
| P1（确认后） | 追加录音 + 重录 UI + 播放器段落标记 |

### 导出功能（P1）

日记是用户的私有数据，必须能导出、能脱离 App 独立查看。

**导出格式：**

| 格式 | 内容 | 适用场景 |
|------|------|---------|
| TXT | 纯文本，按天分组 | 最通用，任何设备都能看 |
| Markdown (.md) | 带格式，含日期/时间段标题 | 笔记软件导入（Obsidian / Notion） |
| JSON | 结构化数据，含元数据 | 开发者 / 数据迁移 / 备份 |
| 音频原文件 (WAV) | 原始录音 | 留档 / 换设备 |

**导出范围：**
- 导出全部日记
- 按日期范围导出（如：2026年4月）
- 单条导出

**导出方式：**
- 分享到其他 App（微信/邮件/笔记软件）
- 保存到本地文件系统
- 导出为 ZIP 压缩包（文本 + 音频打包）

### 文件命名规范（关键设计）

**核心原则：音频文件名与文字条目通过统一 ID 关联，脱离 App 也能对应。**

命名格式：`{日期}_{时间}_{序号}.{扩展名}`

```
VoiceDiary_Export_2026-05-01/
├── diary.md                      # 所有文字（Markdown）
├── diary.txt                     # 所有文字（纯文本）
├── diary.json                    # 结构化数据
└── audio/
    ├── 20260501_0932_001.wav     # ← 对应文字中的 _001
    ├── 20260501_1845_002.wav
    └── 20260430_2315_003.wav
```

**文字中的引用方式：**

Markdown / TXT 中每条日记标注对应音频文件名：
```
📅 2026-05-01

🌅 上午 09:32 | 01:23 | 🎵 20260501_0932_001.wav
今天项目启动会，心情不错...

🌆 傍晚 18:45 | 00:47 | 🎵 20260501_1845_002.wav
下班路上想起一个事...
```

JSON 中每条记录包含 `audioFileName` 字段：
```json
    {
      "id": "20260501_0932_001",
      "createdAt": "2026-05-01T09:32:00",
      "updatedAt": "2026-05-01T10:15:00",
      "durationSeconds": 83,
      "transcribedText": "今天项目启动会...",
      "audioFileName": "20260501_0932_001.m4a",
      "isTranscribed": true,
      "isCompressed": true
    }
```

**设计要点：**
- 文件名本身包含日期时间，肉眼可辨认
- 三位序号 `_001` 解决同一天多条的重名问题
- 文字和音频用完全相同的文件名前缀，只需看名字就知道谁对应谁
- 不依赖任何数据库或 App，用文件管理器就能配对

**TXT 导出示例：**
```
========== 语音日记导出 ==========
导出时间：2026-05-01 10:30
总条目：127 条
==================================

📅 2026-05-01
─────────────

🌅 上午 09:32 | 01:23 | 🎵 20260501_0932_001.wav
今天项目启动会，心情不错，跟团队把需求对了一遍...

🌆 傍晚 18:45 | 00:47 | 🎵 20260501_1845_002.wav
下班路上想起一个事，明天得跟小李确认一下...

📅 2026-04-30
─────────────

🌙 深夜 23:15 | 02:10 | 🎵 20260430_2315_003.wav
睡不着，想了很多关于未来的事...
```

**JSON 导出示例：**
```json
{
  "exportedAt": "2026-05-01T10:30:00",
  "totalCount": 127,
  "entries": [
    {
      "id": "20260501_0932_001",
      "createdAt": "2026-05-01T09:32:00",
      "durationSeconds": 83,
      "transcribedText": "今天项目启动会，心情不错...",
  "audioFileName": "20260501_0932_001.wav"
    }
  ]
}
```

### 明确不做

| 功能 | 原因 |
|------|------|
| Emoji 手动选心情 | 增加操作摩擦，用户随便选导致数据失真，情绪应由 AI 从文字推断 |
| 序号标序 | 档案思维不符合日记场景，用时间段分组更自然 |
| 摇一摇录音 | 误触发率高（走路/坐车/手抖），Widget/快捷方式更可靠 |
| 社交分享 | 日记是私密记录，不做朋友圈/微博等社交平台直接分享；P1 分享功能仅限「分享给个人」（微信好友/邮件/笔记软件），不走社交平台 API |
| 录音编辑/裁剪 | 日记强调即时记录而非精修，剪辑增加复杂度，不符合"零摩擦"理念 |
| 定时录音 | 场景极窄，闹钟类 App 可替代，与"灵感闪现即录"定位不符 |
| 多人协作 | 日记是个人工具，多人编辑与隐私保护矛盾 |
| 录音转视频 | 功能边界外，需引入视频渲染引擎，包体和复杂度大增 |

### 备份与恢复（P0）

**设计原则：用户主动控制，简单可靠。**

| 操作 | 说明 |
|------|------|
| 立即备份 | 设置页点击「立即备份」→ 将 SQLite 数据库文件 + 全部音频文件打包为 ZIP → 写入 Documents/VoiceDiaryBackup/ → 新备份覆盖旧备份（只保留最新一份） |
| 从备份恢复 | 设置页点击「恢复备份」→ 读取 Documents/VoiceDiaryBackup/ 中的 ZIP → 解压 → 覆盖当前数据库和音频文件 → 重启 App 生效 |
| 恢复前确认 | 恢复前弹窗提示「当前数据将被替换，是否继续？」，防止误操作 |

**备份文件结构：**

```
Documents/VoiceDiaryBackup/
└── voicediary_backup.zip        # 只保留这一份
    ├── voicediary.db            # SQLite 数据库完整拷贝
    └── audio/
        ├── 20260501_0932_001.wav
        └── ...
```

**设计要点：**
- 不做自动定时备份：iOS 后台任务限制严格，Android 厂商 ROM 杀后台，自动备份不可靠，给用户虚假安全感
- 只保留最新一份：避免备份文件占用过多空间（音频文件会持续增长）
- 备份的是数据库原文件 + 音频原文件：恢复后 100% 还原，包括转写状态、编辑记录等所有数据
- 备份/恢复期间禁止录音和转写操作，防止数据不一致

### 设置页面（P0）

设置页是 App 唯一的全局配置入口，**极简设计，仅保留必需项**：

```
⚙️ 设置

── 隐私 ──────────────
  应用锁          [开启 ▾]
  自动锁定        [1 分钟 ▾]
                  └ 立即锁定 / 30 秒 / 1 分钟 / 5 分钟 / 从不

── 存储 ──────────────
  存储空间        1.2 GB / 可用 64 GB
                  ├── 音频    1.1 GB
                  ├── 日记正文  12 MB
                  └── 其他      8 MB
  [立即备份]
  [恢复备份]

── 快捷录音 ──────────
  ☑ 桌面 Widget
  ☑ 耳机双击触发
  （通知栏快捷按钮常驻，无需显示）

── 关于 ──────────────
  版本            v1.0.0
  [开源许可]
  [发邮件反馈]
```

**设计要点**：
- 设置项均带当前值预览，点击进入修改，减少页面跳转
- 存储区简洁显示总量和主要分类，技术细节隐藏
- 移除 MVP 不需要的配置（音频质量、录音时长、云端同步、清理功能等）
- 通知栏快捷按钮默认开启，不占设置位
- 回收站入口仅在列表页底部显示，设置页不重复

**移除的设置项（MVP 不做或不需要配置）**：
- 最短录音时长：固定 1 秒
- 音频质量：固定 16kHz 16bit Mono
- 长按音量键/悬浮球：P1 功能
- 触发行为：固定直接录音
- 云端同步配置：P2 功能
- 存储位置：固定本地优先
- 清理音频：压缩功能完成后自动清理，无需手动
- 导出日记：P1 功能

### 存储空间管理（P1）

**设计原则：用户掌控数据，展示简洁。**

**1. 空间统计**

设置页「存储」区简洁显示：

```
存储空间      1.2 GB / 可用 64 GB
             ├── 音频    1.1 GB（已压缩 M4A + 待压缩 WAV）
             ├── 日记正文  12 MB
             └── 其他      8 MB（数据库索引/缓存等）
[清理存储空间]
```

**移除的统计项**：
- 回收站占用：合并到其他
- FTS 索引单独统计：合并到其他

**2. 清理策略**

| 操作 | 说明 |
|------|------|
| 清理已压缩 WAV 原文件 | 转写+压缩完成后，WAV 原文件可删除（M4A 已保留），单次可释放大量空间 |
| 选择条目删除 | 进入多选模式，勾选后删除（经回收站） |
| 清空回收站 | 物理删除所有已软删除条目及音频，不可恢复 |
| 按日期范围删除 | 选择"3 个月前"/"6 个月前"/"1 年前"，批量删除旧日记 |

**3. 云端同步（P1）**

| 云服务 | 平台 | 说明 |
|--------|------|------|
| iCloud | iOS | 系统原生，用户无感 |
| OneDrive | Android / iOS | 跨平台，Android 首选 |

**同步策略：**
- 仅同步数据库 + 已压缩的 M4A 音频（不同步 WAV 原文件，节省带宽）
- 增量同步：基于 `UpdatedAt` 字段，只上传变更的条目
- 冲突处理：MVP 阶段采用 Last-Write-Wins（最新修改时间优先），P1 可增加冲突检测和用户选择
- 同步时机：WiFi + 充电时自动同步（可配置）；也可手动触发
- 同步状态：设置页显示最近同步时间 + 同步状态

**4. 存储位置自动切换**

```
录音保存时
  → 检查本地剩余空间
  → 剩余空间 > 200MB → 正常本地保存
  → 剩余空间 < 200MB → 弹窗警告「存储空间不足」
    → 选项 A：立即清理（跳转清理页）
    → 选项 B：切换云端存储（需已配置云同步）
    → 选项 C：继续本地保存（可能写入失败）
  → 云端存储模式：录音仍先写入本地临时文件 → 上传云端 → 删除本地音频文件
  → 数据库元数据始终保留在本地（条目列表和搜索不受影响）
  → 播放云端音频时：先下载到临时缓存 → 播放 → 缓存自动清理
```

**5. 空间不足降级策略**

| 空间状态 | 行为 |
|----------|------|
| 充足（> 500MB） | 正常运行 |
| 偏低（200-500MB） | 设置页显示黄色警告，建议清理 |
| 紧张（< 200MB） | 录音前弹窗警告，提供清理/云端选项 |
| 临界（< 50MB） | 暂停转写（转写产生临时数据），仅保留录音功能 |

### 数据迁移方案（P0）

SQLite 数据库 schema 变更是不可避免的，必须从 MVP 开始建立迁移机制。

**迁移策略：版本号 + 增量迁移脚本**

```csharp
public class DatabaseMigration
{
    public int Version { get; set; }           // 目标版本号
    public string Description { get; set; }    // 迁移描述
    public Action<SQLiteConnection> Up { get; set; }   // 升级脚本
    public Action<SQLiteConnection> Down { get; set; } // 降级脚本（可选）
}
```

**执行流程：**

```
App 启动 → 打开数据库 → 读取 user_version（SQLite PRAGMA）
  → user_version == 0 → 执行全量建表（首次安装）
  → user_version < 当前版本 → 按顺序执行增量迁移 [v1→v2] → [v2→v3] → ...
  → user_version == 当前版本 → 无需迁移
  → 迁移完成 → 更新 user_version
```

**设计要点：**
- 每次迁移在事务中执行，失败则回滚，数据库不会处于中间状态
- 迁移脚本只允许 `ALTER TABLE ADD COLUMN`，不允许 `DROP COLUMN` 或 `RENAME`（SQLite 支持有限且危险）
- 需要删列的场景：新代码忽略旧列即可，不物理删除
- 首次安装直接执行全量建表，不走迁移脚本
- `user_version` 从 1 开始，每次发版涉及 schema 变更时 +1

---

## 四、日记展示设计

按天 + 时间段自动分组，不使用序号。**列表连续展示，按日期分组，虚拟滚动优化性能**。

```
📅 今天

🌅 上午 09:32
   "今天项目启动会，心情不错……"
   ▶ 01:23

🌆 傍晚 18:45
   "下班路上想起一个事……"
   ▶ 00:47

📅 昨天

🌙 深夜 23:15
   "睡不着，想了很多……"
   ▶ 02:10

📅 2026 年 4 月
─────────────
[显示 30 条记录]
```

**列表策略：连续滚动 + 虚拟分页**

| 设计点 | 说明 |
|--------|------|
| 初始加载 | 最近 30 条记录 |
| 滚动加载 | 滚动到底部自动加载最近 30 条（无限滚动） |
| 分组方式 | 今天/昨天/本周/上周/更早（按月分组标题） |
| 月份指示器 | 顶部显示当前可见数据的月份（如「2026 年 5 月」） |
| 月份选择器 | P1 功能：点击顶部月份可快速跳转 |
| 性能优化 | MAUI CollectionView 虚拟化，仅渲染可见区域 |

时间段划分：
- 🌅 上午：06:00 - 11:59
- ☀️ 下午：12:00 - 17:59
- 🌆 傍晚：18:00 - 20:59
- 🌙 深夜：21:00 - 05:59

---

## 五、技术方案

### 技术栈

| 层面 | 选型 | 理由 |
|------|------|------|
| 跨平台框架 | .NET MAUI (C#) | 技术栈统一，单代码库 |
| 语音转文字 | sherpa-onnx (端侧) | 完全离线、中文效果好、跨平台一致、封装 Whisper 前后处理、C# NuGet 直接用 |
| 模型大小 | whisper-tiny (~40MB) | sherpa-onnx 内置，平衡识别率与包体大小 |
| 本地数据库 | SQLite + sqlite-net-pcl | MAUI 生态成熟，轻量可靠 |
| 音频格式 | WAV (PCM 16bit Mono 16kHz) | Plugin.Maui.Audio 仅支持 PCM 录制，零延迟落盘；16kHz 为 Whisper 最优采样率，体积可通过转写后异步压缩解决 |
| UI 组件 | .NET MAUI Community Toolkit | 现成组件，减少重复造轮子 |

### 离线转写方案对比（已决策）

| | 路线 A：平台原生 | 路线 B：sherpa-onnx + Whisper ✅ |
|--|-----------------|--------------------------------|
| 原理 | iOS Speech / Android SpeechRecognizer | sherpa-onnx 端侧跑 Whisper 模型 |
| 中文效果 | 一般，标点弱 | 好，带标点 |
| 离线支持 | Android 好，iOS 需下载语言包 | 完全离线 |
| 包体影响 | 无 | +40~80MB（量化模型） |
| 跨平台一致性 | 差（两个平台表现不同） | 好（同一模型） |

**决策：选择路线 B（sherpa-onnx + Whisper）**。离线优先 + 中文 + 跨平台一致性是核心诉求。

### 录音格式决策（已决策）

**选择方案：PCM 直写 WAV，转写后异步压缩 M4A。**

| | 路线 A：直接录 M4A (AAC) | 路线 B：PCM 录 WAV + 异步压缩 ✅ |
|--|--------------------------|--------------------------------|
| 录制启动延迟 | 高（AAC 编码器初始化需 100-300ms） | 极低（PCM 零编码，按下即写） |
| 落盘可靠性 | 差（M4A 容器需 moov atom 收尾，中断则文件损坏不可播放） | 好（WAV 头先写，数据追加式，中断后已写部分可播放） |
| 存储占用 | 小（1 分钟约 0.5MB） | 大（1 分钟约 1.9MB，16kHz Mono 16bit） |
| 插件支持 | Plugin.Maui.Audio 不支持 AAC 编码录制 | 原生支持 PCM 录制 |
| 转写兼容性 | 需转码为 PCM 后送 Whisper | 直接送入，16kHz 为 Whisper 最优采样率 |
| 压缩时机 | 不需要 | 转写完成后后台异步压缩为 M4A 并删原 WAV |

**录音参数**：Mono / 16bit / 16kHz（Whisper 最佳采样率，无需重采样，语音质量足够，1 分钟约 1.9MB）

**核心思路**：录音阶段追求极致的速度和可靠性（PCM 零编码延迟 + 中断安全），存储问题交给异步压缩解决。用户感知：按住即录无延迟，松手即存不丢失，空间占用在后台自动优化。

**录音→转写→压缩 完整异步流程**：

```
用户松开录音按钮
  → [同步] PCM 流写入完成，WAV 文件已落盘 → 数据库写入 DiaryEntry（IsTranscribed=false, IsCompressed=false）
  → [异步] 转写队列：WAV → sherpa-onnx 推理 → 更新 TranscribedText, IsTranscribed=true
  → [异步] 压缩队列：WAV → 平台原生编码器 → M4A → 验证 M4A 可播放 → 更新 IsCompressed=true → 删除原 WAV 文件
  → [异步] 更新 FTS 索引
```

**关键设计：转写先于压缩**

- 转写使用 WAV 原始文件（16kHz，无需重采样，Whisper 最优输入）
- 压缩在转写完成后执行（转写优先级 > 压缩优先级）
- 压缩前验证：如果 WAV 文件被意外删除，跳过压缩，数据库保持 `IsCompressed=false`
- 压缩后验证流程：
  1. 验证 M4A 文件可打开（尝试获取时长/元数据）
  2. 验证 M4A 文件大小 > 0
  3. 验证 M4A 文件大小 < WAV 文件（压缩有效）
  4. 对比时长：|M4A 时长 - WAV 时长| < 0.5s
  → 全部通过 → 删除 WAV，更新 `IsCompressed=true`，`AudioFileName` 从 `.wav` 改为 `.m4a`
  → 任一失败 → 保留 WAV，记录 `TranscribeError`，下次 App 重启重试
- 压缩失败：保留 WAV，不重试压缩（不影响核心功能，下次 App 重启时扫描重试）
- 并发控制：录音开始 → 检查是否有压缩任务在运行 → 有则暂停压缩 → 录音完成后恢复

**AudioFileName 字段定义**：包含完整扩展名

| IsCompressed | AudioFileName 示例 | 实际文件 |
|-------------|-------------------|---------|
| false | `20260501_0932_001.wav` | WAV 文件存在 |
| true | `20260501_0932_001.m4a` | M4A 文件存在，WAV 已删除 |

压缩完成时，数据库中 `AudioFileName` 字段从 `.wav` 更新为 `.m4a`（UPDATE 操作）。

### 转写策略（已决策）

**选择方案 1：录完后台异步转写。**

- 录音结束 → 先保存原始音频 → 异步启动 Whisper 推理
- 转写完成 → 更新数据库 → 推送通知 / UI 自动刷新
- 避免用户等待推理时间（中端手机 1 分钟音频约 10-30 秒）

**转写队列持久化与恢复**：

转写队列不依赖内存状态，而是通过数据库查询动态重建：

```
App 启动 → 查询 IsTranscribed=false AND IsDeleted=false 的条目
  → 按 CreatedAt 排序 → 加入转写队列
  → 依次异步转写
```

- 无需额外的队列表：`IsTranscribed` 字段本身就是持久化的队列标记
- App 被杀掉后重启：自动扫描未转写条目，重新入队
- 转写中断的条目：`TranscribeAttempts` 已递增，下次空闲时自动重试
- 最大重试次数：3 次，超过后标记 `TranscribeError` 并停止重试，用户可手动触发重新转写
- **转写队列优先级调整（P1）**：
  - 列表页长按条目 → 「优先转写」→ 该条目前移到队列首位
  - 设置页「转写队列管理」入口（高级功能）：查看当前等待转写的条目列表
  - **禁止取消转写和跳过转写**：确保所有录音最终都有文字稿，避免数据不完整

**模型加载时机**：

```
App 启动 → 后台线程预加载 sherpa-onnx 模型（不阻塞 UI）
  → 加载完成 → 标记模型就绪，转写队列可开始工作
  → 首次录音时如模型未就绪 → 转写任务排队等待，录音不受影响
```

- **预加载而非懒加载**：App 启动时在后台线程加载模型（约 1-2 秒），用户从打开 App 到按住录音的间隙足够完成加载
- **不阻塞录音**：模型加载是转写的前置条件，不是录音的前置条件。即使模型还没加载完，录音也能正常进行，录完后排队等转写
- **内存常驻**：模型加载后不卸载，直到 App 被系统回收。频繁加载/卸载会导致转写延迟不稳定
- **内存压力时降级**：系统发出低内存警告 → 释放模型内存 → 下次转写时重新加载

### 数据模型

```csharp
public class DiaryEntry
{
    public string Id { get; set; }              // GUID，跨设备唯一
    public DateTime CreatedAt { get; set; }
    public DateTime UpdatedAt { get; set; }      // 最后修改时间（编辑文字/重新转写时更新）
    public string TranscribedText { get; set; }  // 转写结果
    public string AudioFileName { get; set; }     // 音频文件名（WAV，不含路径，路径由服务计算）
    public int DurationSeconds { get; set; }      // 录音时长（秒）
    public bool IsTranscribed { get; set; }       // 是否已完成转写
    public int TranscribeAttempts { get; set; }   // 转写尝试次数
    public string TranscribeError { get; set; }   // 最后一次转写错误信息
    public bool IsCompressed { get; set; }        // 音频是否已异步压缩为 M4A（节省空间）
    public bool IsDeleted { get; set; }           // 软删除标记（回收站机制）
    public DateTime? DeletedAt { get; set; }      // 删除时间，用于回收站自动清理
}
```

- **主键用 GUID**：避免自增 ID 在跨设备同步/备份恢复时冲突
- **只存文件名不存路径**：路径由 `IStorageService` 根据平台计算，迁移设备不会断
- **UpdatedAt 追踪修改**：手动编辑转写文字、重新转写时更新，支持增量同步和冲突检测
- **软删除 + 回收站**：`IsDeleted` + `DeletedAt` 实现回收站机制，30 天后自动物理删除
- **录音存 WAV，转写后异步压缩**：PCM 直写零延迟，转写完成后后台将 WAV 压缩为 M4A 节省空间（约 1/10），`IsCompressed` 标记当前文件格式
- **转写状态完善**：记录尝试次数和错误信息，支持重试和问题排查
- SQLite 存文字元数据，音频文件存本地文件系统
- **预留 AudioSegment 表**：MVP 时建表但 UI 不暴露，为后续「多段录音合并」功能做准备（详见可选功能章节）

### 项目结构

```
VoiceDiary/
├── Views/
│   ├── RecordPage.xaml          # 首页 - 录音（App 入口）
│   ├── DiaryListPage.xaml       # 日记列表
│   └── DiaryDetailPage.xaml     # 详情 + 回放
├── ViewModels/
│   ├── RecordViewModel.cs
│   ├── DiaryListViewModel.cs
│   └── DiaryDetailViewModel.cs
├── Services/
│   ├── IAudioRecorder.cs        # 录音服务接口
│   ├── IAudioCompressor.cs      # 音频压缩接口（WAV→M4A）
│   ├── ISpeechRecognizer.cs     # 语音识别接口
│   ├── WhisperRecognizer.cs     # sherpa-onnx 实现
│   └── DiaryRepository.cs       # SQLite CRUD
├── Models/
│   └── DiaryEntry.cs
├── Converters/
│   └── DateTimeToPeriodConverter.cs  # 时间→时段emoji
└── Resources/
    └── Models/
        └── whisper-tiny.zh.onnx     # 中文模型
```

### 全文搜索方案

**选型：SQLite FTS5 + CJK 双字符分词**

```sql
CREATE VIRTUAL TABLE DiaryEntry_FTS USING fts5(
    TranscribedText,
    content='DiaryEntry',
    content_rowid='rowid',
    tokenize='unicode61'  -- 基础分词器，CJK 自动双字符滑动窗口
);
```

**设计要点：**
- **FTS5 外部内容表**：索引与数据分离，不重复存储原文，节省空间
- **CJK 分词**：`unicode61` 对 CJK 字符自动做双字符滑动窗口（bigram），无需额外分词库，中文搜索可用
- **搜索范围**：仅搜索 `IsTranscribed = true` 的条目，未转写的不纳入
- **日期过滤**：FTS 匹配后通过 `JOIN DiaryEntry WHERE CreatedAt BETWEEN` 过滤日期范围
- **索引维护**：应用层手动维护（转写完成 / 手动编辑文字时）
  ```sql
  REPLACE INTO DiaryEntry_FTS(rowid, TranscribedText) 
  VALUES (@rowid, @text);
  ```
  或使用触发器自动维护（推荐）：
  ```sql
  CREATE TRIGGER DiaryEntry_AI AFTER INSERT ON DiaryEntry BEGIN
      INSERT INTO DiaryEntry_FTS(rowid, TranscribedText) 
      VALUES (NEW.rowid, NEW.TranscribedText);
  END;
  ```
- **搜索结果高亮**：使用 FTS5 的 `highlight()` 函数标记匹配词
- **索引重建**：设置页提供「重建搜索索引」按钮（高级功能，索引损坏时使用）
  ```sql
  DELETE FROM DiaryEntry_FTS;
  INSERT INTO DiaryEntry_FTS(rowid, TranscribedText)
  SELECT rowid, TranscribedText FROM DiaryEntry WHERE IsTranscribed = true;
  ```

**局限与取舍：**
- bigram 分词不如 jieba 精确（如搜"项目"会匹配含"项目"的，但不会拆成"项"+"目"单独匹配），对日记场景足够
- 不引入额外分词库，保持包体小、零依赖

### 最短录音时长

**阈值：1 秒。低于 1 秒的录音自动丢弃，不保存。**

理由：
- 误触（按住即松开）通常 < 0.3s，1 秒足以过滤
- 1 秒的音频约 32KB (16kHz Mono 16bit)，即使误存也无损
- 低于 1 秒的语音转写结果毫无意义（Whisper 需要至少 0.5s 有效语音）
- 丢弃时给触觉反馈（双短振）+ toast 提示"录音时间过短"，让用户知道发生了什么

---

## 六、风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| Whisper 推理慢 | 1 分钟音频中端机可能需 10-30 秒 | 异步转写 + 通知，用户无需等待 |
| 模型包体大 | App 安装包 +40~80MB | tiny 模型已是最小，可接受；后续可做模型按需下载 |
| MAUI 平台差异 | iOS/Android 录音权限、文件系统不同 | 抽象接口层，平台差异封装在 Service 实现中 |
| SQLite 并发 | 录音和转写可能同时读写 | 使用 WAL 模式 + 异步锁 |
| 录音与转写资源冲突 | 转写占用 CPU/内存时影响录音质量 | 录音绝对优先，转写可暂停 |
| 录音中断 | 来电话/系统杀后台/低电量导致录音丢失 | PCM 流式写入 WAV（逐 chunk 追加），中断时已落盘部分可播放（WAV 容器头先写，数据追加式写入） |
| 转写质量差 | tiny 模型中文识别可能不理想 | 提供重新转写按钮 + 手动编辑 + 保留原始音频兜底 |
| 误触录音 | 短按/误触产生无意义录音 | 录音 < 1 秒自动丢弃 + 触觉反馈提示 |
| 数据丢失 | 手机损坏/丢失/App 卸载 | 手动备份：SQLite + 音频文件打包到 Documents 目录，只保留最新一份，支持从备份恢复 |

### 并发策略：录音优先

录音和转写可能同时发生（用户录完一条，马上录下一条，此时上一条还在转写）。**核心原则：录音优先级最高，转写必须让路。**

```
用户按下录音
  → 检查是否有转写任务正在运行
  → 有 → 暂停/挂起转写任务 → 释放 CPU 和内存
  → 开始录音（独占资源）
  → 松开 → 保存录音
  → 恢复之前的转写队列 → 继续排队转写
```

**转写队列设计**：
- 所有待转写的日记按录制时间 FIFO 排队
- 同一时刻只运行一个转写任务
- 录音开始 → 立即暂停当前转写
- 录音结束 → 恢复队列，被中断的转写任务从头重新开始（Whisper 为非流式模型，无法续推，必须整条重新推理）
- 转写失败 → 标记 `IsTranscribed = false`，下次空闲时自动重试

**用户感知**：
- 正在转写的条目显示"转写中..."（带进度指示）
- 录音期间转写进度冻结，录完自动恢复
- 用户无感知切换，不需要手动管理

---

## 七、隐私保护（已决策）

**必须有，但要做到"该锁时锁，不该锁时别碍事"。**

### 设计原则

- 录音保存是后台操作，不能因为锁屏而丢录音
- 快捷入口（Widget/通知栏）录音不应有解锁摩擦
- 锁屏验证不应打断"打开即录"的核心体验

### 实现方案：分级解锁

| 场景 | 需要验证 | 说明 |
|------|---------|------|
| App 刚打开 | ✅ | 第一道防线 |
| 切后台 30 秒内回来 | ❌ | 查个消息回来不烦人 |
| 切后台超过 1 分钟 | ✅ | 防别人直接看内容 |
| Widget / 通知栏快捷录音 | ❌ | 快捷入口零摩擦 |
| 查看历史日记 / 回放 | ✅ | 敏感内容受保护 |
| 录音完成自动保存 | ❌ | 后台操作，用户无感知 |
| 正在录音时切后台回来 | ❌ | 录音状态不应被打断 |

### 锁屏 UI 设计

不做单独锁屏页。打开 App 时，录音按钮已经在背景就位，指纹验证以浮层形式呈现：

```
┌─────────────────────┐
│                     │
│     🎙️ (大按钮)     │  ← 背景就是录音页
│                     │
│  ┌───────────────┐  │
│  │  请验证指纹    │  │  ← 浮层，验证完自动消失
│  │   👆          │  │
│  └───────────────┘  │
│                     │
└─────────────────────┘
```

验证通过 → 浮层消失，用户看到的就是录音按钮。**感知上没有"进入"的延迟。**

### 验证失败兜底

- 指纹连续失败 3 次 → 可切换密码
- 全部失败 → 不显示日记内容，仅显示录音按钮

### 锁屏超时可配置（设置页）

```
自动锁定：[切后台超过 1 分钟 ▾]
          └ 立即锁定
            30 秒
            1 分钟（默认）
            5 分钟
            从不（不推荐）
```

### 技术实现

- Android: `BiometricPrompt` API
- iOS: `LocalAuthentication` (LAContext)
- MAUI: 使用 **CommunityToolkit.Maui** 的 `IBiometricAuth` 接口（非 MAUI Essentials 内置，Essentials 无生物识别 API），或通过平台特定代码（Handler）直接调用原生 API

---

## 八、快捷录音（已决策）

**核心思路：提供多种触发方式，用户在设置中选择自己习惯的。**

### 可选触发方式

| 触发方式 | 平台 | 优先级 | 说明 |
|----------|------|--------|------|
| **通知栏快捷按钮** | **Android / iOS** | **P0（默认常驻）** | **始终可用的兜底入口** |
| 桌面 Widget | Android / iOS | P0（设置页可选） | 桌面 1x1 小组件，点击即录 |
| 耳机按钮触发 | Android / iOS | P1 | 双击开始/停止，取决于平台支持（iOS 限制较多） |
| 长按音量键 | Android | P1 | 锁屏状态下长按音量上键触发 |
| App 内快捷按钮 | Android / iOS | P0 | 打开 App 直接就是录音页（默认） |

**移除的触发方式（MVP 不做）**：
- 悬浮球/悬浮窗：增加系统权限申请，MVP 不做

**设计要点**：
- 通知栏按钮常驻，无需配置
- 耳机触发标注「取决于平台支持」，避免用户期望过高

### 不采用的方案

| 方案 | 原因 |
|------|------|
| 语音唤醒词（类似"嘿Siri"） | iOS 限制第三方后台语音唤醒；Android 厂商 ROM 杀后台；始终监听耗电严重 |
| 后台检测人声自动录 | 电量消耗极高；隐私风险大；误触发率高；应用商店审核可能拒绝 |
| 摇一摇 | 误触发率高（走路/坐车/手抖）；体验不够确定 |

### 设置页配置

**通知栏快捷按钮为默认常驻功能，无需配置，始终开启。**

以下为可选增强方式（仅 P0 功能）：

```
增强快捷方式：（可多选）

☑ 桌面 Widget        ← P0，推荐
☐ 耳机双击触发       ← P1，标注「取决于平台支持」

（通知栏快捷按钮常驻，无需显示）

触发后直接开始录音，无需验证。
```

**移除的配置项**：
- 长按音量上键：P1 功能，MVP 不显示
- 悬浮球：P1 功能
- 触发行为选择：固定直接录音

### 各方式设计要点

**通知栏快捷按钮（默认常驻，不可关闭）**
- 常驻通知，带"开始录音"操作按钮
- 录音中通知变为"录音中... 00:32 [停止]"
- 后台录音，不影响正常使用手机
- 作为所有快捷方式的兜底，确保任何场景都有录音入口

**桌面 Widget**
- 提供 1x1 尺寸，仅一个录音图标
- 点击 → 直接开始录音（绕过锁屏验证）
- 录音中 Widget 变为红色/动态效果，显示时长
- 再次点击 → 停止录音并保存
- 无需打开 App 主界面

**耳机按钮触发（推荐）**
- 最自然的"手不方便时"录音方式
- 双击耳机按钮 → 开始录音；再次双击 → 停止并保存
- 可配置为双击或长按触发（避免与切歌冲突）
- 支持有线耳机、蓝牙耳机、AirPods
- 锁屏、口袋里、走路中均可使用
- 零额外电量消耗
- Android：通过 `MediaSession` / `MediaButtonReceiver` 拦截
- iOS：通过 `MPRemoteCommandCenter` 处理

**长按音量键**
- 仅音量上键，避免与系统音量调节冲突
- 长按阈值：0.5 秒（可配置）
- 需要前台服务监听按键事件
- 锁屏状态下也可触发
- 注意：部分厂商 ROM 可能拦截音量键事件，需适配

**悬浮球（Android）**
- 小圆形悬浮按钮，可拖动调整位置
- 不使用时半透明，不遮挡内容
- 点击开始录音，录音中变红并显示时长
- 需要 `SYSTEM_ALERT_WINDOW` 权限

### 快捷录音与隐私的协调

- 所有快捷方式触发的录音：**不验证身份**（录音本身不是泄密）
- 快捷录音完成后想查看转写结果：**需要解锁**（查看内容需要保护）
- Widget / 通知栏录音完成 → 通知提示"录音已保存，点击查看"

---

## 九、开发节奏（预估）

**并行策略轨道图：**

```
关键路径轨道（串行依赖，13 天）：
Sprint 1 → Sprint 2 → Sprint 3 → Sprint 4 ──→ Sprint 7
(3 天)    (3 天)    (3 天)    (2 天)         (2 天)
录音+回放  转写+重试  列表+搜索  UI 打磨 + 删除   导出 + 备份

并行轨道（可与上方并行，3-5 天）：
Sprint 5 ──→ Sprint 6
(1-2 天)     (2-3 天)
隐私保护     快捷录音

总计：3 周可出完整版本
```

| 阶段 | 目标 | 预估时间 | 前置依赖 | 可并行 |
|------|------|----------|----------|--------|
| Sprint 1 | 录音 + 回放 + 边录边写 + 取消手势 + 最短时长过滤 | 3 天 | 无 | - |
| Sprint 2 | 接入 sherpa-onnx 离线转文字 + 重试机制 + 模型预加载 | 3 天 | Sprint 1 | - |
| Sprint 3 | 日记列表 + 详情页 + 时间段分组 + 全文搜索 + 按月分页 | 3 天 | Sprint 1 | Sprint 5 |
| Sprint 4 | UI 打磨 + 手动修正文字 + 删除（回收站） | 2 天 | Sprint 3 | Sprint 5 |
| Sprint 5 | 隐私保护（生物识别锁）+ 设置页 | 1-2 天 | 无 | Sprint 3, 4 |
| Sprint 6 | 快捷录音（Widget + 通知栏 + 耳机 + 音量键） | 2-3 天 | Sprint 1 | Sprint 5 |
| Sprint 7 | 导出 + 手动备份/恢复 + 存储空间管理 | 2 天 | Sprint 3 | - |
| Sprint 8（后期） | 云端同步（iCloud / OneDrive） | 3 天 | Sprint 7 | 后期迭代 |

**并行策略**：
- Sprint 5（隐私保护）无前置依赖，可在 Sprint 3/4 期间并行开发
- Sprint 6（快捷录音）依赖 Sprint 1 的录音服务接口，可在 Sprint 2 之后与 Sprint 3/4 并行
- 实际关键路径：S1 → S2 → S3 → S4 → S7，约 13 天
- 加上并行，预计 **3 周可出完整版本**
- 云端同步作为辅助功能，安排在后期 Sprint 8（可选）

---

## 十、待定 / 开放问题

- [ ] Whisper 模型最终选 tiny 还是 base？需实测中文识别效果再定
- [ ] 多段录音合并功能：数据模型已预留，功能开发待定（后期迭代，锦上添花）
- [ ] 云端同步功能：P2 辅助功能，后期完善（Sprint 8）
- [x] 长录音方案：已确定，按住 + 上滑锁定（微信/WhatsApp 模式）
- [x] 隐私保护：已确定，分级解锁 + 生物识别
- [x] 快捷录音：已确定，通知栏常驻 + 桌面 Widget（MVP）+ 耳机触发（P1，取决于平台）
- [x] 导出功能：已确定，支持 TXT / Markdown / JSON + 音频原文件导出
- [x] 录音交互：已确定，按住为主 + 上滑锁定 + 上滑取消 + 触觉反馈，短按 (<0.3s) 过滤
- [x] 录音格式：已确定，PCM 直写 WAV（零延迟 + 中断安全），转写后异步压缩 M4A 节省空间
- [x] 数据模型：已确定，GUID 主键 + 相对路径 + 转写重试状态
- [x] 备份策略：已确定，用户手动备份（SQLite + 音频打包 ZIP 到 Documents），只保留最新一份，支持恢复
- [x] 全文搜索：已确定，SQLite FTS5 + unicode61 CJK bigram 分词，仅搜索已转写条目
- [x] 最短录音时长：已确定，< 1 秒自动丢弃 + 触觉反馈提示
- [x] 设置页面：已确定，极简 4 分区（隐私/存储/快捷/关于），移除 MVP 不需要的配置
- [x] 数据迁移：已确定，PRAGMA user_version + 增量迁移脚本，只 ADD COLUMN 不 DROP
- [x] 转写队列持久化：已确定，基于数据库 IsTranscribed 字段动态重建队列，App 重启自动恢复；支持优先级调整，禁止取消/跳过
- [x] 存储空间管理：已确定，简洁展示 + 云端同步（P2）
- [x] 模型加载时机：已确定，App 启动后台预加载，不阻塞录音，内存常驻
- [x] 录音→压缩流程：已确定，PCM录WAV → 转写(用WAV) → 压缩为M4A → 删WAV，AudioFileName随格式更新
- [x] 生物识别API：已确定，使用 CommunityToolkit.Maui 的 IBiometricAuth（MAUI Essentials 无此功能）
- [x] 删除机制：已确定，左滑软删除→回收站 (30 天)→自动物理删除，支持撤销按钮（3 秒）+ 列表页底部入口
- [x] 列表分页：已确定，连续滚动 + 虚拟分页，顶部月份指示器，P1 快速跳转
- [x] 开发节奏：已确定，标注依赖关系，关键路径13天+并行可3周出完整版

---

## 十一、开源项目选型（已调研）

> 最后更新：2026-05-01

### 核心模块

| 模块 | 推荐项目 | GitHub 地址 | 许可证 | 说明 |
|------|----------|-------------|--------|------|
| 🗣 语音转文字 | **sherpa-onnx** | <https://github.com/k2-fsa/sherpa-onnx> | Apache 2.0 | 跨平台离线语音识别框架，原生支持 C# API，支持 Whisper / Moonshine / SenseVoice 多种模型，10.9K+ ⭐，性能优于直接 ONNX Runtime |
| 🎙 录音 | **Plugin.Maui.Audio** | <https://github.com/jfversluis/Plugin.Maui.Audio> | MIT | MAUI 官方音频插件，跨平台录音 + 播放封装，NuGet 直接安装 |
| 💾 数据库 | **sqlite-net-pcl** | <https://github.com/praeclarum/sqlite-net> | MIT | 轻量 ORM，MAUI 生态最成熟，1.8K+ ⭐ |
| 🔒 生物识别 | **CommunityToolkit.Maui** | <https://github.com/CommunityToolkit/Maui> | MIT | 提供 `IBiometricAuth` 跨平台生物识别接口，MAUI Essentials 无此功能，需依赖社区工具包 |

### UI 与工具

| 模块 | 推荐项目 | GitHub 地址 | 许可证 | 说明 |
|------|----------|-------------|--------|------|
| 🧩 UI 组件 | **.NET MAUI Community Toolkit** | <https://github.com/CommunityToolkit/Maui> | MIT | 弹窗、动画、手势、SnackBar 等现成组件，2.4K+ ⭐ |
| 📅 日历视图 | **XCalendar** | <https://github.com/ME-MarvinE/XCalendar> | MIT | 跨平台日历控件，支持 MAUI，高度可定制，332 ⭐ |
| 📤 导出 ZIP | **SharpZipLib** | <https://github.com/icsharpcode/SharpZipLib> | MIT | .NET 标准 ZIP 压缩库，导出打包用 |

### 产品参考

| 项目 | 地址 | 参考价值 |
|------|------|----------|
| **ALog** | iOS App Store 搜索 "ALog AI 语音日记" | 产品思路高度一致（录音→转文字→AI 整理），UI 交互设计可借鉴，仅 iOS Swift 实现不可直接复用 |

### 录音交互参考（按住 + 上滑锁定）

> 录音按钮交互（按住录音、上滑锁定、上滑取消）需要自研，无现成 MAUI 组件可直接复用。以下为有价值的参考资源：

| 资源 | 类型 | 地址 | 参考价值 |
|------|------|------|----------|
| **MAUI 模仿微信"按住-说话"** | CSDN 博文 + 源码 | <https://blog.csdn.net/jevonsflash/article/details/130174276> | ⭐⭐⭐ **最直接参考**。用 MAUI PanGesture 实现了完整的按住说话交互：PanContainer 手势控件 + PitGrid 目标区域 + 拖拽/激活/取消动画 + 状态机 |
| **TouchTracking.MAUI** | NuGet 包 | <https://github.com/nor0x/TouchTracking.MAUI> | ⭐⭐ 统一的多点触控 API，封装了 MAUI Touch 事件（Pressed/Moved/Released + 坐标），可作为手势底层 |
| **react-native-whatsapp-audio-recorder** | React Native | GitHub: `NadeemIqbal/react-native-whatsapp-audio-recorder` | ⭐ 完整实现了 WhatsApp 录音交互（按住录音、左滑取消、上滑锁定），交互逻辑可参考但不能直接用，MIT 许可证 |
| **flutter_plugin_record** | Flutter (329 ⭐) | <https://github.com/yxwandroid/flutter_plugin_record> | ⭐ 仿微信语音录制，架构思路可参考，Apache 2.0 许可证 |

**实现思路**：MAUI 的 `PanGestureRecognizer` + `PointerGestureRecognizer` 组合：
- `PointerPressed` → 开始录音
- `PanChanged` (DeltaY < -50px) → 上滑锁定
- `PanChanged` (进入取消区域) → 取消状态
- `PointerReleased` → 根据当前状态决定：停止保存 / 锁定不停止 / 取消

### 许可证合规说明

**全部推荐项目均为宽松开源许可证（MIT / Apache 2.0），商用无限制。**

| 许可证 | 核心要求 | 适用项目 |
|--------|---------|----------|
| **MIT** | 保留版权声明 | Plugin.Maui.Audio、sqlite-net-pcl、MAUI Essentials、CommunityToolkit、XCalendar、SharpZipLib、Whisper 模型 |
| **Apache 2.0** | 保留版权声明 + 许可证副本 | sherpa-onnx |

**合规建议：**
- 在 App 的"关于"页面或设置页底部列出所用开源组件及许可证
- 在项目根目录放置 `THIRD_PARTY_LICENSES.txt` 文件，包含各组件的版权声明
- Apache 2.0 项目（sherpa-onnx）需额外注明修改内容（如有）

### 选型决策说明

**语音转文字为何选 sherpa-onnx 而非直接 ONNX Runtime：**
- sherpa-onnx 封装了 Whisper 的前后处理（音频重采样、mel 特征提取、token 解码），直接 ONNX Runtime 需要自己实现这些
- 内置流式解码支持，推理性能经过优化
- 提供现成的 C# NuGet 包，Android/iOS 均有预编译 native 库
- 支持多种模型（Whisper、Moonshine、SenseVoice），后续可平滑切换更好的模型
- 社区活跃（10.9K+ ⭐），问题响应快

**技术栈总览：**
```
语音转文字：sherpa-onnx (Whisper tiny 中文模型)   [Apache 2.0]
录    音：Plugin.Maui.Audio (PCM→WAV)           [MIT]
音频压缩：平台原生 MediaCodec / AVFoundation    [系统 API]
数 据 库：sqlite-net-pcl                        [MIT]
生物识别：CommunityToolkit.Maui (IBiometricAuth) [MIT]
UI 组件：MAUI Community Toolkit                [MIT]
日历控件：XCalendar                             [MIT]
导出压缩：SharpZipLib                           [MIT]
```
